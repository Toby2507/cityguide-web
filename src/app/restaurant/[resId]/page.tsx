interface Props {
  params: {
    resId: string;
  };
}

const RestaurantDetailPage = ({ params: { resId } }: Props) => {
  return <div>RestaurantDetailPage</div>;
};

export default RestaurantDetailPage;
