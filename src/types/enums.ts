export enum Roles {
  Admin = 'admin',
  User = 'user',
}

export enum Stays {
  Hotel = 'hotel',
  Apartment = 'apartment',
  Hostel = 'hostel',
  Resort = 'resort',
  Bnb = 'bnb',
}

export enum Parking {
  FREE = 'Free',
  PAID = 'Paid',
  NO = 'No',
}

export enum PropertyType {
  STAY = 'Stay',
  RESTAURANT = 'Restaurant',
  NIGHTLIFE = 'NightLife',
}
